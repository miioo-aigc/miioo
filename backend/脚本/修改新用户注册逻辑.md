# 修改新用户注册逻辑

> **Workspace:** /Users/xingyi/Desktop/迭代一版_副本

---

**User**

/plan 新用户注册时，用户名默认为创作者，而不是手机号，把帮我修一下后端已有的逻辑

**Assistant**

先做只读排查，确认当前“新用户注册默认用户名”到底落在哪段后端逻辑，再基于实际文件写出可执行方案。

正在收敛到具体实现点，我会先定位注册路由、用户模型和现有测试，确认默认用户名是在哪一层被手机号覆盖的。

已经定位到核心点：`backend/app/routers/auth.py` 里的 `_resolve_default_nickname()` 现在在未传昵称时直接回退成手机号，这正是问题根因。接着我补齐请求模型和现有文档口径，确认计划里要改的范围与验证项。

计划已经足够明确，不需要再反问你了。现在我把“代码改哪里、文档回写哪里、怎么验证”整理成正式计划文件，供你确认后我再开始执行。

我再补一眼文档现有写法，确保计划文件里的路径和回写项完全按仓库惯例来写，然后就发你审阅。

我发现当前默认昵称逻辑是共享在一个 helper 里，除了 `POST /api/auth/register`，还会影响“验证码首次自动建号”和“微信扫码绑定后首次自动建号”。这个范围会直接影响计划，我先把口径和你确认一下。

范围已经锁定为“所有首次建号入口统一默认成 `创作者`”。我现在生成计划文件，并把实际涉及的后端文件、测试和文档回写项都落进去。

计划文件已生成，内容已经按你确认的“全部首次建号入口统一改”为准，包含实际代码落点、测试补点和文档回写范围。你确认后，我就按这份计划直接开始改代码。

开始执行，先按计划回读一遍计划文件并建立待办，然后依次改后端逻辑、补测试、回写文档并做校验。

先改共享默认昵称入口，这一步会同时覆盖密码注册、验证码首次建号和微信首次建号；然后我再把对应测试和文档同步起来。

默认昵称逻辑已经改完，接下来在两条真实建号路径上补测试锁定行为，避免后面又被回退成手机号。

代码和文档都已改完，我现在跑两组聚焦测试并做诊断检查，确认这次改动没有带出新的后端问题。

校验已经通过，最后我再回读一下改动片段，整理成可直接交付的变更清单和验证结果。

**已完成**
- 已把共享默认昵称逻辑从“回退为手机号”改为“回退为 `创作者`”，实际落点在 [auth.py](file:///Users/xingyi/Desktop/迭代一版_副本/backend/app/routers/auth.py#L170-L174)，因此会统一覆盖密码注册、验证码首次自动建号、微信首次自动建号。
- 已同步更新注册 schema 描述，明确未传昵称时默认使用 `创作者`，见 [auth.py](file:///Users/xingyi/Desktop/迭代一版_副本/backend/app/schemas/auth.py#L9-L16)。
- 已补充回归测试，锁定验证码首次建号默认昵称行为，见 [test_auth_user_creation.py](file:///Users/xingyi/Desktop/迭代一版_副本/backend/tests/test_auth_user_creation.py#L33-L50)。
- 已补充微信首次建号测试，并保留“显式传入昵称优先”的既有语义，见 [test_wechat_auth_flow.py](file:///Users/xingyi/Desktop/迭代一版_副本/backend/tests/test_wechat_auth_flow.py#L121-L185) 和 [test_wechat_auth_flow.py](file:///Users/xingyi/Desktop/迭代一版_副本/backend/tests/test_wechat_auth_flow.py#L190-L248)。

**文档**
- 已更新后端接口说明中的“注册”和“验证码登录”默认昵称口径，见 [BACKEND_API_DOC.md](file:///Users/xingyi/Desktop/迭代一版_副本/backend/BACKEND_API_DOC.md#L381-L384) 和 [BACKEND_API_DOC.md](file:///Users/xingyi/Desktop/迭代一版_副本/backend/BACKEND_API_DOC.md#L458-L464)。
- 已按你的规则补记接口行为变更，见 [接口变动文档.md](file:///Users/xingyi/Desktop/迭代一版_副本/接口变动文档.md#L3-L31)。
- 已更新进度与变更记录，见 [module-progress.md](file:///Users/xingyi/Desktop/迭代一版_副本/docs/plans/module-progress.md#L26-L29)、[项目进度文档.md](file:///Users/xingyi/Desktop/迭代一版_副本/docs/plans/项目进度文档.md#L44-L46)、[CHANGELOG.md](file:///Users/xingyi/Desktop/迭代一版_副本/CHANGELOG.md#L3-L5)。
- 本轮没有前端代码改动，所以未更新 `前端改动记录.md`。

**验证**
- 已执行聚焦测试：`DATABASE_URL=sqlite+aiosqlite:////tmp/test.db SECRET_KEY=test-secret ENCRYPTION_KEY=0123456789abcdef0123456789abcdef PYTHONPATH=backend ./backend/.venv/bin/pytest -q backend/tests/test_auth_user_creation.py backend/tests/test_wechat_auth_flow.py`
- 测试结果：退出码 `0`。
- 已检查诊断：`backend/app/routers/auth.py`、`backend/tests/test_auth_user_creation.py`、`backend/tests/test_wechat_auth_flow.py` 均无新增诊断问题。

**结果**
- 现在新用户在未传昵称时，默认用户名会是 `创作者`，不再是手机号。
- 这个规则已经统一覆盖你确认的全部首次建号入口。

---

