# 前端改动记录

## 2026-06-29：管理员控制台功能移植

### 概述
将 `frontend_new` 中的管理员控制台功能完整移植到 `frontend` 工程，包括管理员 API、管理页面组件、全局错误处理等。

### 新增文件（19个）

#### API 层
- `src/api/admin.js` — 管理员 API（Banner推荐图、社群二维码、卡片可见性、模型开放管理、用户账号管理）
- `src/api/error.js` — 统一错误处理（getDisplayErrorMessage、shouldPromoteErrorToDialog、openGlobalErrorDialog、GLOBAL_ERROR_DIALOG_EVENT）

#### 工具函数
- `src/utils/voicePreviewPlayer.js` — 音色试听播放器（toggleVoicePreview、stopVoicePreview、subscribeVoicePreview）

#### Admin 组件（14个）
- `src/components/admin/adminShared.jsx` — 共用组件（Toggle、ActionButton、TextInput、SelectInput、PaginationControls）
- `src/components/admin/AdminConsoleSidebar.jsx` — 左侧导航栏
- `src/components/admin/AdminOverviewPanel.jsx` — 总览面板
- `src/components/admin/AdminDisplaySettingsPage.jsx` — 展示配置页（Banner、二维码）
- `src/components/admin/AdminModelVisibilityPage.jsx` — 模型开放管理页
- `src/components/admin/AdminModelVisibilityFilters.jsx` — 模型开放筛选器
- `src/components/admin/AdminModelVisibilityTable.jsx` — 模型开放表格
- `src/components/admin/AdminVoiceLibraryPage.jsx` — 音色库管理页
- `src/components/admin/AdminVoiceLibraryFilters.jsx` — 音色库筛选器
- `src/components/admin/AdminVoiceLibraryTable.jsx` — 音色库表格
- `src/components/admin/AdminAccountManagementPage.jsx` — 账号管理页
- `src/components/admin/AdminAccountFilters.jsx` — 账号筛选器
- `src/components/admin/AdminAccountTable.jsx` — 账号表格
- `src/components/admin/AdminAccountEditModal.jsx` — 账号编辑弹窗

#### 通用组件 & 页面
- `src/components/GlobalErrorDialog.jsx` — 全局错误弹窗
- `src/pages/AdminConsolePage.jsx` — 管理员控制台主页面

### 修改已有文件（4个）

#### `src/utils/cacheKeys.js`
- 新增 admin 缓存 key：`adminCommunityQr`、`adminModelVisibility`、`adminAccounts`、`adminModelVisibilityPrefix`、`adminAccountsPrefix`
- 新增 `voiceLibraryPrefix`（音色库缓存失效用）

#### `src/api/voices.js`
- 新增大量音色数据处理辅助函数（性别推断、年龄推断、语言推断、显示名生成等）
- 新增 `getVoiceDisplayName(voice)` — 音色显示名获取
- 新增 `matchesVoiceQueryFilters(voice, filters)` — 音色筛选匹配
- 新增 `apiCreateVoiceLibraryItem` — 创建音色库条目
- 新增 `apiUpdateVoiceLibraryItem` — 更新音色库条目
- 新增 `apiDeleteVoiceLibraryItem` — 删除/停用音色
- 新增 `apiCreateCustomVoice` — 创建自定义音色
- 增强 `apiGetVoiceLibrary` — 支持更多查询参数（is_enabled、include_disabled、page、pageSize）
- 增强 `apiGetVoices` — 支持 language 参数

#### `src/components/AccountMenu.jsx`
- 新增 `IconAdmin` 盾牌图标组件
- 新增 `isAdmin` prop — 控制管理员入口显示
- 新增 `onOpenAdminConsole` prop — 管理员控制台导航回调
- 新增"管理员控制台"菜单项（仅 isAdmin 为 true 时显示）

#### `src/pages/Home.jsx`
- 新增 `useCallback` 导入
- 新增 `AdminConsolePage` 导入
- `showToast` 改为 `useCallback` 包裹，type=error 时调用 shouldPromoteErrorToDialog 判断是否升级为全局错误弹窗
- 新增 `globalError` 状态和 GLOBAL_ERROR_DIALOG_EVENT 事件监听
- 新增 `closeGlobalErrorDialog` 函数
- `activeKey` 初始化排除 'admin'（防止非管理员刷新进入后台）
- `activeKey` useEffect 排除 'admin' 持久化 + 新增管理员卫士（非管理员访问 admin 自动弹回首页）
- 新增 `handleOpenAdminConsole` 函数
- 首页栏 AccountMenu 新增 `isAdmin`、`onOpenAdminConsole` props
- WorkflowHeadbar 使用 `handleOpenAdminConsole` 替代内联回调
- 新增 `activeKey === 'admin'` 渲染块（AdminConsolePage）

---

## 2026-06-29（续）：剪辑页面（StoryboardPage）能力迁移

### 概述
将 `frontend_new` 中 StoryboardPage（分镜/剪辑页面）的全部增强能力移植到 `frontend`，包括旁白配音系统、统一下载能力、真人素材接入、音色库集成，并为「剪辑」tab 补齐独立页面入口。

### 新增文件（4个）

#### API 层
- `src/api/download.js` — 统一下载辅助（buildDownloadFilename、downloadBlob、downloadFromUrl、downloadFile、downloadMediaFile 等）
- `src/api/storyboardNarration.js` — 旁白配音系统（normalizeNarrationFromStoryboard、buildNarrationGenParams、resolveVoiceNameForRole、clampDurationLabel、DEFAULT_NARRATION_EMOTIONS）
- `src/api/liveMaterials.js` — 真人素材 API（上传、分组、素材管理、认证回调事件）

#### 组件层
- `src/components/LiveMaterialPickerModal.jsx` — 真人素材选择弹窗（认证、上传、分组选择、拖拽上传）

### 修改已有文件（8个）

#### `src/pages/StoryboardPage.jsx`
- **整体替换**为 frontend_new 版本（7321 行，旧版 6717 行）
- 新增 `isEditMode` prop：编辑模式下隐藏剧集选择器
- 新增旁白配音系统：segments + globalVoiceParams + emotion + gen_params
- 新增统一下载能力：通过 download.js 进行分镜图/视频下载
- 新增真人素材支持：LiveMaterialPickerModal 接入
- 新增音色库集成：voiceList 贯穿 NarrationColWrapper → ShotRow → NarrationCol → NarrationItem

#### `src/api/storyboard.js`
- 新增 `apiDownloadStoryboardImage` — 分镜图下载接口
- 更新 `apiDownloadStoryboardVideo` — 支持 `rawResponse` 参数
- 更新 `apiBatchDownloadStoryboardImages` — 支持 `rawResponse` 参数

#### `src/pages/Home.jsx`
- 新增 `GLOBAL_ERROR_DIALOG_EVENT, openGlobalErrorDialog, shouldPromoteErrorToDialog` 导入
- 新增 `LIVE_MATERIAL_AUTH_COMPLETED_EVENT` 导入
- 新增 `GlobalErrorDialog` 组件导入
- 新增 `liveMaterialRefreshToken` 状态 + 真人认证完成事件监听
- 新增 `globalError` 状态 + GLOBAL_ERROR_DIALOG_EVENT 监听
- `showToast` 新增 error 类型升级为全局弹窗逻辑
- WorkflowHeadbar 新增 `onOpenAdminConsole` prop
- AccountMenu 新增 `isAdmin`、`onOpenAdminConsole` props
- StoryboardPage 渲染条件从 `activeStep === 'storyboard'` 改为 `(activeStep === 'storyboard' || activeStep === 'edit')`
- StoryboardPage 新增 `liveMaterialRefreshToken`、`isEditMode` props
- CreationPage 新增 `liveMaterialRefreshToken` prop

#### `src/utils/cacheKeys.js`
- 同步 frontend_new 版本：新增 admin 缓存 key、models 函数 availableOnly 签名

#### `src/utils/imageUrl.js`
- 同步 frontend_new 版本：新增 resolveOrigin/getManagedMediaOrigin/rewriteManagedMediaUrl 等媒体 URL 归一化逻辑

#### `src/utils/modelAdapter.js`
- 同步 frontend_new 版本：新增 supportsLiveMaterial、generationModes、supportsReferenceImage 等能力字段
